<!DOCTYPE html>

<title>Product</title>

{% block body %}
    <h1>Product</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ product.id }}</td>
            </tr>
            <tr>
                <th>Category</th>
                <td>{{ product.category }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_first_index') }}">back to list</a>

    <a href="{{ path('app_first_edit', {'id': product.id}) }}">edit</a>

    {{ include('first/_delete_form.html.twig') }}
{% endblock %}
