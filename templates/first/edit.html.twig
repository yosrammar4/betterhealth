<!DOCTYPE html>

<title>Edit Product</title>

{% block body %}
    <h1>Edit Product</h1>

    {{ include('first/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_first_index') }}">back to list</a>

    {{ include('first/_delete_form.html.twig') }}
{% endblock %}
